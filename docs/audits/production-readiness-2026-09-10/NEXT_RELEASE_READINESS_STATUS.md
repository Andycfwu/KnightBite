# KnightBite Draft PR #1 — protected hosted verification

Updated September 10, 2026, 23:04 UTC. **Not ready for production approval. Production must remain on `70166c4fd04bb0d5ca25b792283924eff36c1f31`.** No merge, promotion, production deployment or shared-setting change occurred. Original uncommitted UI files and index were preserved. [Previous runtime report](READINESS_BEFORE_HOSTED_VERIFICATION.md).

## Verified candidate and exact checks

Application commit **C1 `8f6b6230b724d4ff8df0aef90c0067bc6de019a7`**, tree `96705dfd8cad612dd054029f7b25d78b82caad40`, is published in [Draft PR #1](https://github.com/Andycfwu/KnightBite/pull/1). This report/evidence update changes documentation only; final publication read-back will identify its new PR head, matching Preview and exact checks in the local report and PR body after results exist. Do not infer those later results from C1.

- Matching **Ready Preview**: [knight-bite-rfgxc7s1h-andycfwus-projects.vercel.app](https://knight-bite-rfgxc7s1h-andycfwus-projects.vercel.app), deployment **`dpl_C8H7wuyE6saK1Uj9YzuMhwNrPGiE`**, completed 22:56:12 UTC. Normal `npm run build`; no fixture environment or auth bypass. [Deployment/runtime evidence](hosted-evidence/candidate-preview.json).
- Exact C1 Linux push [34539771767](https://github.com/Andycfwu/KnightBite/actions/runs/34539771767) and PR integration [34539776038](https://github.com/Andycfwu/KnightBite/actions/runs/34539776038) passed. Actual PR checkout **T `c8704a5da20e35f8a84cff7ae01db1ad31b8abd0`** has the same tree as C1. Both required jobs ran the full gate on **24.21.0 and 24.19.0**, each with 249 unit and 28 browser checks, types, lint, isolated Production and Preview builds. [Push excerpts](hosted-evidence/candidate-push-excerpts.txt), [PR excerpts](hosted-evidence/candidate-pr-excerpts.txt).
- Same two full gates passed locally, with no live provider traffic or delivered test events. Fresh npm audit found zero dependency vulnerabilities; actionlint and whitespace checks passed. Console log trailing whitespace was normalized in this documentation update, without changing test results. [Local source digest/results](hosted-evidence/local-verification.json), [UI preservation](hosted-evidence/preservation-before-handoff.json).

## Function runtime and effective headers

**Actual function runtime is now observed independently of build runtime.** The dynamic `/preview-check` invocation at 22:56:50.992 UTC and the Preview menu-loader function log at 22:56:13.812 UTC both reported exact C1, region **iad1**, Linux x64, **Node 24.19.0 / OpenSSL 3.5.7 / Undici 7.29.0 / llhttp 9.4.3**. The separate build record at 22:55:38.367 UTC reported those versions plus Next 15.5.25 / sharp 0.35.4. Vercel's dashboard itself exposes only managed Node 24.x, not a patch selector or attestation of every instance. These request observations do not certify every function, future patch, platform internals or native runtime libraries.

The reviewed Node floor and [component-advisory assessment](../../RUNTIME_POLICY.md) remain applicable. The bundle contains versions affected by later OpenSSL/Undici advisories whose required APIs were not found reachable in the app. Do not call the entire runtime fully patched; reassess new reachable issues and adopt newer supported patches when available.

Authenticated same-origin GETs verified actual response headers. `/profile` returned 200, `public, max-age=0, must-revalidate`, PRERENDER; `/hall/atrium` returned 200, `private, no-cache, no-store, max-age=0, must-revalidate`, MISS. Both returned HSTS `max-age=63072000; includeSubDomains; preload`, CSP without `unsafe-eval`, DENY, nosniff, strict-origin referrer policy and denied camera/microphone/geolocation. The observed hashed JavaScript asset returned `public,max-age=31536000,immutable` and the configured security headers; **HSTS was null on that asset response**. Existing CSP `unsafe-inline` remains documented. This is a sample of effective responses, not every edge/TTL cycle. [Header records](hosted-evidence/candidate-effective-headers.json).

## Hosted application verification and limits

[Detailed observations](hosted-evidence/candidate-hosted-workflows.json), 22:56–23:02 UTC, used the authenticated Codex in-app browser. Its exact engine version is not exposed by the available API. Chromium/WebKit automated results remain separate local/CI evidence.

| Hosted case | Actually observed |
| --- | --- |
| Atrium real success/partial | Normal route returned Breakfast, **237 food cards**; seven map groups totaled 237. Requested/header date was Thursday, September 10; returned retrieval label was 6:56 PM. All nutrition was incomplete or variable. No sample food. |
| Actual upstream degradation | Bounded daily summary recorded breakfast parsed, lunch/dinner menu timeouts, six timed-out nutrition labels and 232 skipped enrichments. 238 normalized inputs, zero dropped, one deduplicated, 237 returned. Daily provider duration 37,763 ms is distinct from the unchanged 15-second application display deadline. A later shared load can complete after a caller gives up. |
| Normal Atrium unavailable | Later ordinary Halls → Atrium navigation displayed “Menu status unavailable” and the unavailable message in map and list modes, zero food cards, no retrieval timestamp or backup/sample label, with the requested date intact. No upstream failure injection was used. Exact request-to-attempt causality is not established; ordinary caching/prefetch applies. Previously added real Busch plate items correctly survived navigation. |
| Date boundary | Hosted `/preview-check/unavailable` explicitly rendered a controlled null result for UTC **2026-09-11T00:30:00Z**, requested Rutgers **2026-09-10**. Header showed Thursday, September 10. This tests the hosted view/utilities; it is not a real midnight/provider rollover. |
| Nutrition and plate | Atrium unknown/variable items kept Unknown totals. Busch plain bagel displayed 323 kcal, 13g protein, 70g carbs, 1.5g fat. Adding an unknown assorted bagel preserved a known subtotal; doubling the plain bagel produced 646 kcal, 26g/140g/3g. Source-provided zero carb presentations remained distinct. This verifies presentation/arithmetic, not independent nutritional accuracy or safety. No different-size same-name portion pair occurred in the observed Busch breakfast; offline variant tests passed. |
| Temporary state and controls | Add/increase/decrease/remove/clear worked. Plate survived client navigation and reset on reload. Keyboard traversal cycled all eight native-dialog buttons forward and wrapped backward, Escape closed and returned focus to the opener. Test preference changed by Space, reported saved, survived reload, then was cleared using the app control. |
| Mobile and navigation | Busch 375×812 Explore/Menu/My plate/Halls and Atrium 320×700 map/list/plate worked; document scroll widths equaled 375/320. Map's intentional internal scrolling remained. Viewport reset afterward. |
| Homepage and console | Observed “4 menus confirmed for today,” with operating hours separately labeled typical/unverified. No all-unknown homepage was observed; offline mixed/deadline tests remain separate. No captured browser errors/warnings. |

## Preview-only analytics and changes

`DeploymentAnalytics` omits the SDK only for Vercel-managed **Preview**; existing Production analytics stays enabled. The hosted DOM contained no analytics script. Offline browser tests fail on any external/analytics transport and passed; no synthetic event was delivered. No complete hosted network trace is available, so this is source-level suppression evidence plus observed SDK absence, not a claimed interception log.

Small guarded `/preview-check` pages expose only allowlisted public versions and same-origin response headers; a controlled unavailable view contains no food fixture. Scoped middleware rejects diagnostics before streaming outside Preview (HTTP 404); page guards remain. A once-per-process Preview menu-runtime log is protected by try/catch. Normal menus, caches, deadlines, parsing, identities, station maps and storage behavior were not changed. Package scripts extend the existing required gate, without new dependencies. [Procedure](../../PREVIEW_VERIFICATION.md), [unsigned human checklist](../../RELEASE_HUMAN_SIGNOFF.md).

**Never promote the Preview-built artifact into Production.** Static pages retain their build environment; build the approved source normally under Production so analytics is enabled and diagnostics unavailable. This is not a request to disable deployment protection or alter production variables. [Vercel environment semantics](https://vercel.com/docs/environment-variables/system-environment-variables).

## Recovery target: verified source, no eligible patched Instant Rollback

Pinned patched **recovery-source candidate is C1 / `dpl_C8H7wuyE6saK1Uj9YzuMhwNrPGiE` / the immutable Preview above**. It preserves the current application, truthful failures and security fixes, with both runtime gates and hosted verification recorded. No database migration exists; plates are temporary and browser preferences retain version 1. It is **not a Production artifact or eligible Instant Rollback target**, and its Preview analytics behavior makes direct promotion unsuitable. Deployment retention currently shows 30 days; revalidate availability before relying on any artifact.

The actual current Hobby rollback picker was opened read-only and cancelled: it offers **`dpl_Cw8UgwpjjaqwStBK9FTqus6tn33D`, commit `906b5fd20433acb16b848a3eaf536cba9795a47b`**, prior to current 70166c4. Its lockfile is pre-remediation; it is rejected. After the first patched release, 70166c4 would become the predecessor and is also rejected. **No patched production-eligible rollback target or rollback rehearsal exists.** Creating/activating one would require separately authorized production work; no Preview is mislabeled as eligible. [Read-only picker evidence](hosted-evidence/rollback-inspection.json), [Vercel Hobby eligibility](https://vercel.com/docs/instant-rollback).

Recovery procedure for separate approval: an appointed operator records the incident/source/deployment; creates an isolated recovery branch from current main; prepares a reviewed forward recovery using the pinned C1 application as a reference while retaining any later security fixes; runs both complete gates and reviews the new exact SHA. After explicit recovery approval, use the protected main-driven **Production rebuild**, verify main-check/deployment SHA and aliases, then headers, diagnostics 404, analytics behavior with local transport blocking, truthful menus and temporary plates. Do not force-promote or source-revert to the old vulnerable app. Once two suitable patched production artifacts exist, verify the dashboard's exact eligible deployment and rehearse Instant Rollback; rollback disables Auto-Assign, so review pending builds and obtain approval before restoring it. [Operations runbook](../../OPERATIONS_RUNBOOK.md).

## Remaining blockers, owners and separately approved release

1. **Recovery/operations owner decision:** establish an eligible patched target/rehearsal, or explicitly accept the first-patch forward-recovery limitation for an attended limited release. Name operator, backup/contact and 30-minute monitoring coverage. Hobby has no verified automated menu-outage alert.
2. **Human signoffs:** actual Safari/VoiceOver, 200% page zoom and measured map/photo contrast; dietary/source and asset review; privacy/service identity/contact, analytics/log retention and deletion, regions/subprocessors, dashboard access/IP visibility and notices. The concise [checklist](../../RELEASE_HUMAN_SIGNOFF.md) is unsigned; Andy must accept or appoint these roles. Technical checks are not human or legal approval.
3. **Remaining source evidence:** independent Busch/Neilson representative weeks, Livingston breakfast/dinner and peak/seasonal weeks, schools payload and complete Atrium menu/label captures with date/identity/transport provenance. This hosted 237-item observation strengthens conservation evidence but does not bound all accepted weeks or verify failed labels. Do not silently truncate or raise limits.
4. **Final release approval and lifecycle:** protections remain strict named GitHub check/app 15368, accepted temporary zero approvals, admins included, no force/delete and conversation/linear-history rules. Vercel check remains **Production / Blocking**, team 2FA enforced; no setting weakened. Main is the production branch with Auto-Assign ON. A merge may automatically build and assign production. A separate approved release must observe pending-to-success gating and prove **successful main-push actual checkout SHA = final main M = Vercel deployment SHA**, then domain assignment. This lifecycle has not been exercised and cannot be claimed from saved settings or Preview checks. Never bypass with Force Promote.

**Readiness improved:** protected hosted runtime, effective headers, real success/unavailable behavior and major browser workflows now have evidence. **Production remains not approved/not ready pending the specific recovery and human decisions above.** Correctly dated stored-real-menu recovery remains future work.
