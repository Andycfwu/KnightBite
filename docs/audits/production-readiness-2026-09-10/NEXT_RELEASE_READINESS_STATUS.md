# KnightBite release activation readiness

Updated September 10, 2026. Fresh GitHub inspection: **21:01 UTC**; Vercel UI inspection: **21:00–21:09 UTC**; production HEAD: **21:04 UTC**. Local evidence has its own timestamps. This report supersedes the earlier personal-MFA finding; the [prior report](READINESS_BEFORE_ACTIVATION.md) and its evidence are retained as history.

**Verdict: local release preparation and verification are complete; production activation is not authorized.** Personal Vercel 2FA is now verified. The patched candidate is isolated and committed, but GitHub/Vercel gates remain unapplied and the public deployment remains old. Publication/protection activation is the next approval, followed by separate merge/release approval. A broader release still needs the specific operational, privacy and manual evidence below.

## Freshly verified state

| Area | Current evidence / consequence |
| --- | --- |
| GitHub | Public `Andycfwu/KnightBite`; main **`70166c4fd04bb0d5ca25b792283924eff36c1f31`**, unprotected; protection endpoint explicitly reports not protected; no rulesets, remote workflows, main check runs or open PRs. Only successful status is Vercel. One admin/writer collaborator. |
| Repository security | Dependabot alerts and security updates disabled. Secret scanning/push protection enabled. Actions enabled; default token read-only and cannot approve PRs. GitHub personal-2FA field was unavailable in the authenticated user API; its state is not inferred from Vercel enrollment. Auto-merge off. GitHub Production environment has no protection/branch restrictions; it does not govern the Vercel Git integration. |
| Personal Vercel security | Sole Owner appears under **2FA Enabled**, not Disabled; security dashboard independently confirms all team members have 2FA. User reports recovery codes saved. **No secret/recovery page or value was inspected or requested.** |
| Team security | **Two-Factor Authentication Enforcement OFF**; one Owner, zero pending invitations. Require Verified Commits OFF. Dashboard/drain IP visibility ON. Personal enrollment and team enforcement are separate controls. The dashboard's “all recommended protections” message is not release approval. |
| Production identity | Hobby team `andycfwus-projects`, project `knight-bite`; alias **knightbitenb.vercel.app** is a current domain of Ready/Production deployment **`dpl_BT9hARabzozdRpqMutPju4P2xF5F`**, source **70166c4**, created 03:19:13 UTC. The local remediation is **not deployed**. |
| Release trigger | Production branch **main**; **Auto-Assign Custom Production Domains ON**; **No checks configured**. GitHub import is available in the unsaved Add Checks chooser. Main updates can currently build and promote production without the release check. |
| Build/runtime | Build/install command overrides OFF; Node **24.x**, Fluid Compute, **iad1**, 1 vCPU/2 GB, Standard Protection shown for current deployment. Exact Node patch/native binaries remain unverified on production. |
| Analytics | **Enabled**, as requested. Dashboard says no custom events; Speed Insights is not enabled. No analytics setting was changed. Tests intercept transport and send no events. |
| Effective headers | Safe HEAD `/profile`: 200/HIT, HSTS and `public, max-age=0, must-revalidate`; new CSP/frame/nosniff/referrer/permissions headers still absent. No live hall request, malicious image URL or cache-bypass probe was made. |

Evidence: [GitHub](activation-evidence/github-current.json), [Vercel/security](activation-evidence/vercel-current.json), [production headers](activation-evidence/production-headers.json). Read-only settings inspection and local tests are distinct: dashboard navigation is not an instrumented test of platform telemetry.

The current immutable deployment is unchanged, so earlier deployed-source evidence remains applicable: Next **15.5.15**, React/DOM **19.2.5**, sharp **0.34.5**, root PostCSS **8.5.9** and Next's transitive **8.4.31**, old next.config without the new restrictions. Earlier settings evidence (20:25 UTC) for 300-second project duration, 30-day deployment retention, no deploy hooks, unavailable Hobby Alerts/Drains and disabled team AI training was **not freshly re-read**. Those mutable settings could change independently; see [earlier Vercel record](next-release-evidence/vercel-current.json). Local dependency fixes are not evidence of patched production binaries or of exploit reachability.

## Exact local candidate and preserved work

Branch **`codex/release-readiness-2026-09-10`**, worktree **`/Users/andywu/Desktop/Codex/Nutrition-release-readiness`**. No push, merge, setting change, deployment, credential rotation or paid-service action occurred.

| Commit | Scope |
| --- | --- |
| Existing ancestor `1fbe14b7805c95d69bffda11913b4739f8ce8db5` | Previously committed dining-hall overhaul/no-mock/ingestion work; parent is current main. This is part of the complete production-to-candidate diff. |
| **`83161520e6a3a3a41699db3a68b53d6f2810f90e`** | Snapshot of intended remediation, current dining/account UI, station maps/assets, tests and release workflow. 85 files changed relative to the existing ancestor. |
| **`37d5fa1e015a5627554ad1c0e3a0ad7e55ab3423`** | Focused Safari fix found during fresh verification: traverse every drawer control explicitly for Tab/Shift-Tab. Existing assertions reproduced focus leaving for BODY and now check the fix. No layout redesign. Application tree **`27b0de01dc28ba536ef454adb8182d987c104b0c`**. |
| Following documentation commit | Audit history, sanitized verification evidence, both exact protection alternatives and activation/rollback handoff. No application, dependency, fixture, test or workflow change after the tested application commit. Final branch HEAD is presented with the approval request; resolve it with `git rev-parse codex/release-readiness-2026-09-10`. |

The original checkout stays on `codex/dining-hall-overhaul`; its existing source files and index were snapshot-checked and preserved. Candidate changes were made in the separate worktree. Only the readiness handoff documents/evidence are copied back for discoverability. Do not confuse the original dirty checkout or its old `.next` with the release candidate.

Technical review covered shared loader/no-mock callers, immediate log summaries and deadlines, URL/redirect restrictions, nutrition/identity/storage/labels, cache and ingestion budgets, client imports, UI integration and the fixture harness. No app route imports the mock provider or fixtures. Development-only diagnostics remain deliberately callable and are not used by user routes. This assistant review is not an independent human PR approval. The complete finding mapping and deferred domain questions remain in [REMEDIATION_STATUS.md](REMEDIATION_STATUS.md).

The activation-only workflow change adds `codex/release-*` push checks and records/asserts checkout SHA/tree. This closes the gap between candidate **C**, PR test-merge **T**, and final main **M**; Vercel must gate a successful **main-push** check on the same **M** as the deployment. A prior candidate check or a local build cannot stand in for that run. Exact settings and sequencing are in the [activation plan](RELEASE_ACTIVATION_PLAN.md).

## Candidate verification

Fresh **macOS arm64 / Node 24.21.0** run of `npm ci` and **`npm run check` passed** on application commit `37d5fa1`: **242 unit tests**, TypeScript, noninteractive lint, isolated `npm run build`/types and **20 Chromium/WebKit workflows**. Build ID **`wEiex6PEUmVqBSLlIfnJ_`**, source digest **`6d818346ab25afa85c66b4d47ea637f2b809105d46ae9dc8b16731f182e437b5`**. [Build manifest](activation-evidence/mac-build-manifest.json), [full command output](activation-evidence/release-checks.txt), [browser results](activation-evidence/browser-final.json).

Fresh **Ubuntu 24.04 / Linux x64 / Node 24.21.0** execution on the same application commit also passed as one complete script: clean `npm ci`, **`npm run check` exit 0** (242 unit tests, types, lint, isolated production build, **20 browser workflows**), `npm audit --audit-level=moderate` (**zero vulnerabilities**) and `git diff --check`. A sequential captured-script/keyboard run passed **4 tests**. Build ID **`IDPvlCGtdT0-yCC3l16dp`**. Both builds have the **same source digest** and every source-file hash matches the committed application. [Linux output](activation-evidence/linux-release-checks.txt), [manifest](activation-evidence/linux-build-manifest.json). This was local Linux emulation using the official Playwright image, not a GitHub-hosted run.

The captured Vercel script also passed **4 macOS browser checks**, with **zero delivered analytics events**; the plate-removal workflow passed **three additional macOS WebKit repeats**. Linux native inventory confirms sharp 0.35.4, libheif 1.23.2, libvips 8.18.6 and aom 3.15.0. These are local Linux binaries, not production attestations. [Intercept evidence](activation-evidence/analytics-intercepts.json).

Actionlint **1.7.12 passed**; Dependabot YAML parsed and expected ecosystems were validated. The checkout guard accepts the matching SHA and rejects a different SHA. Credential-pattern scan found no private-key/GitHub/AWS/literal bearer/JWT pattern in candidate text/evidence; match values were not printed and no authentication store was read. This bounded scan is not a universal secret detector. Final source and documentation-only commit checks are recorded in [final verification](activation-evidence/final-verification.json).

Initial failures are retained: first macOS run passed 18/20 browser cases, exposing a repeatable drawer focus failure and one plate-removal click that passed the focused rerun. The unchanged drawer regression drove the small application fix. One attempted captured-script run started before the first server had stopped, so it failed at port ownership without executing tests; subsequent runs are sequential. Two Docker launches stalled; they were interrupted, and the late-created container was subsequently started explicitly. These are not hidden successes. Staging the previously untracked evidence exposed terminal whitespace; candidate log copies were normalized without changing results, with before/after hashes in [evidence-formatting.json](activation-evidence/evidence-formatting.json). Earlier raw logs remain in the original worktree/snapshot. Prior Linux evidence was used to compare the starting source before checks, not to certify the later Safari fix.

All menu fixtures and analytics transport are isolated/intercepted. Isolated artifacts include analytics but must not be deployed; a real release builds the approved source normally. Build/check success does not prove live Rutgers availability. Next 15 still emits its legacy ESLint-plugin detection warning; the separate configured lint gate passes. npm lists unapproved install-script advisories for esbuild/fsevents in the local tool environment; no dependency or global approval policy was changed.

## Exact proposed settings and approval boundaries

Nothing below is applied. Use [proposed-settings.json](activation-evidence/proposed-settings.json) and [RELEASE_ACTIVATION_PLAN.md](RELEASE_ACTIVATION_PLAN.md).

- **GitHub main:** PR required; strict/up-to-date **KnightBite release verification**, GitHub Actions app **15368**; administrators included; conversations resolved; linear history; force push/deletion prohibited. Publish the workflow and observe the real check first. No overlapping ruleset or pre-merge production-deployment dependency.
- **Choose one review policy:** [trusted reviewer](activation-evidence/main-protection-trusted-reviewer.json) requires one independent approval and last-push approval, only after an authorized reviewer is available. [Temporary solo policy](activation-evidence/main-protection-temporary-solo.json) sets **zero approvals and last-push approval OFF**, explicitly accepted by the owner, while retaining the PR/check protections. Review the exception by **October 10, 2026** or when a maintainer is added; no automatic lockout. No reviewer has been invited.
- **Dependabot:** enable alerts/security updates; publish the prepared Monday 05:00 America/New_York weekly npm/Actions update policy with at most five open PRs each; no auto-merge. Preserve current read-only Actions permissions and secret scanning/push protection.
- **Vercel:** import/require the named GitHub check; keep production main and auto-assignment ON for the gate; preserve analytics, Standard Protection and Node 24.x. Verify the saved named check and deployment/check SHA pairing. Enable team 2FA only after rechecking all members are enrolled. Do not enable signed-commit enforcement against unsigned history or change paid services/vendors.

**First approval: publish the final candidate branch, create a draft PR, and activate the explicitly chosen protections/dependency updates/team 2FA; no merge or production release.** The branch push can automatically create a **Vercel Preview**. Wait for remote checks and read back settings, then stop for merge/release approval.

**A main merge is a production-triggering action.** With the proposed gate and auto-assignment ON, successful checks on the new M can automatically promote production. Later merge approval must explicitly include that conditional deployment. If the user requires approving the literal M SHA before promotion, obtain approval for Auto-Assign OFF before merge, inspect M/check/deployment under that manual hold, then request separate approval before reactivation. Vercel's GitHub check gate requires ON; OFF must never be described as that enforced gate. Owners can Force Promote, so bypass discipline still matters. [Vercel deployment-check behavior](https://vercel.com/docs/deployment-checks)

## Remaining evidence, owners and prioritized blockers

1. **Release activation:** old public source still serves users; no remote checks or enforced merge/promotion controls. Phase A approval and real GitHub run/read-back evidence are required. Personal Vercel MFA is resolved; team enforcement and solo-review choice remain unapplied. GitHub personal-2FA needs owner/account UI evidence because the API field was unavailable. Do not repeatedly request enrollment or recovery secrets.
2. **First-patch recovery:** no verified patched production predecessor; Hobby only permits instant rollback to the immediate predecessor. Old `70166c4` is not an approved security fallback. Prepare an owner-approved forward fix if necessary and rehearse recovery between patched artifacts before claiming rollback readiness. See the concrete [activation/production/rollback procedure](RELEASE_ACTIVATION_PLAN.md) and [operations runbook](../../OPERATIONS_RUNBOOK.md).
3. **Ownership:** name/accept service/release operator, incident contact, backup coverage, privacy/notice owner and dietary-label reviewer. Repository ownership does not establish acceptance. Hobby currently lacks configured automatic semantic menu-outage alerts. The proposed five-minute manual review for thirty minutes after release requires an assigned operator; log TTL/cache behavior prevents treating it as a guaranteed detection SLO.
4. **Manual/product evidence:** actual Safari/VoiceOver spoken output, focused-row removal announcements/focus, 200% zoom, contrast across map/photo states, dietary-label semantics and asset permissions. The exact [manual procedure](../../ACCESSIBILITY_QA.md) is ready; WebKit automation is not VoiceOver testing or legal/allergy-safety certification.
5. **Provider/host evidence:** independent Busch/Neilson weeks, Livingston breakfast/dinner and peak/seasonal week, schools response, full Atrium menu/label captures with transport/identity provenance. Existing sanitized Livingston week conserves all seven days’ items within current bounds; it does not cover all sources. No live provider request was made. Exact deployed Node/native inventory, per-function overrides/concurrency behavior, token/bypass metadata and a verified staging smoke remain open.
6. **Privacy decisions with analytics ON:** service identity/contact; applicable notice/consent language; log/analytics retention and deletion terms (including derived records/backups), residency/subprocessors, who can access dashboards/logs, IP visibility choice, and retention of incident evidence. No new vendor/account/database was introduced. Personal 2FA and function region do not resolve these decisions.

**Next action:** approve only Phase A after choosing trusted-reviewer or explicit temporary zero-approval policy. Review the exact final branch HEAD provided in the handoff; no automatic deployment is authorized by this report. Correctly dated stored-real-menu recovery remains future work. Analytics stays enabled; plates stay temporary; failed menus never receive sample-food substitutes.
