# KnightBite remediation status — September 10, 2026

This records the first implementation pass. See [NEXT_RELEASE_READINESS_STATUS.md](NEXT_RELEASE_READINESS_STATUS.md) for the later authenticated deployment inspection, Linux verification, analytics-enabled checks and remaining release protections. The historical evidence below is preserved.

Implementation against `codex/dining-hall-overhaul`, base `1fbe14b7805c95d69bffda11913b4739f8ce8db5`, with pre-existing and concurrent uncommitted UI work preserved. The original audit and remediation plan remain historical evidence. No deploy, push, credential rotation, hosting/repository-setting change, account, database or vendor was added.

Local release readiness has improved, but this is **not broad-release approval**. The external/product gates below are still open. The user explicitly chose **analytics enabled** and **temporary plates**, and identified `knightbitenb.vercel.app`. No service/privacy/release owner was supplied.

## Every audit finding

| Finding / plan action | Status, code evidence and local verification | Remaining reason and specific next action |
| --- | --- | --- |
| **F-001 / A001 — Dependencies** | **Fixed locally.** `package.json`, lockfile, `.nvmrc`: Next 15.5.25, React/DOM 19.2.8, sharp 0.35.4, PostCSS 8.5.28 including Next's transitive copy; Node 24.21.0 LTS. Transitive browserslist/selector/baseline packages also patched. Clean install/build, installed native versions, effective dependency tree and npm audit checked. See advisory assessment and evidence below. | **Requires external evidence:** inspect the deployed Linux/other native artifact and actual alias-to-build mapping. Local patched packages do not patch the currently deployed site. Recheck advisories at the next release. |
| **F-002 / A002 — Atrium destinations** | **Fixed.** `foodpronet-url.ts` validates exact HTTPS origin/label endpoint, location/date/recipe/portion, credentials, query uniqueness and path traversal before cache/network. `rutgers-provider.ts` uses manual redirects and rejects every redirect. `foodpronet-label-identity.ts` rejects conflicting/duplicate explicit returned recipe/date/location fields. `foodpronet-url`, `foodpronet-label-identity` and `provider-hardening` tests prove disallowed destinations never reach fetch and conflicting labels cannot populate a food. | Captured label excerpts lack independent identity metadata. A full approved sanitized label capture is needed before recognizing additional name/title selectors. Existing checks do not certify upstream content accuracy or protect against a compromised intended origin/DNS. |
| **F-003 / A003 — Missing nutrition** | **Fixed.** `types.ts`, `nutrition.ts`, provider and menu/plate components retain per-field `null` versus supplied zero. Complete zero menus survive. Known subtotals include coverage; custom/unknown foods do not become assumed zero. Observed Atrium protein/sodium/sugar markup now parses. Plate snapshots retain date/portion/custom/nutrition. `nutrition-quality`, `provider-hardening`, captured label and browser tests exercise partial, unknown, invalid, zero and mixed totals. | **Requires product/domain review:** numerical/source data remain informational, with no allergy or clinical assurance. Upstream mislabeled portions/nutrition remain possible. |
| **F-004 / A004 — Portion identity** | **Fixed.** `menu-item-identity.ts` deterministically disambiguates colliding variants using full variant hashes, preserves noncolliding IDs and source ordering. `plate.ts` snapshots source identity/date/portion/values. Reordering cannot swap variants; changed nutrition/date does not rewrite an existing plate. Unit/provider/browser checks add 120- and 240-calorie portions separately and confirm 360 calories. | The plate intentionally remains temporary. If source membership changes a formerly colliding ID to noncolliding, its base ID can differ; date/value snapshot separation prevents silently rewriting an earlier entry. No durable cross-session identity promise is made. |
| **F-005 / A005 — Browser storage** | **Fixed.** `preferences-storage.ts`, `useUserPreferences.tsx`, `ProfileScreen.tsx`: guarded access/read/write/delete, bounded validated v1 records, explicit-edit writes, visible save state and session fallback. Valid legacy records migrate on explicit edit; malformed/future records remain untouched until clear. Unit/browser tests cover SecurityError, quota, corruption, wrong/future types, legacy, reload, two-tab writes/deletion and no automatic restoration. | Browser denial can prevent deletion too; UI reports failure and points to browser site-data controls. Simultaneous whole-record writes use last-successful-write semantics, not cross-tab transactions. |
| **F-006 / A006 — Dietary/allergen labels** | **Fixed technical classification.** `dietary-labels.ts`, provider/filter and menu details keep explicit dietary/free-from labels separate from contains/may-contain allergens. Unknown labels remain source text, conflicts are withheld from positive filters, missing labels imply nothing. `dietary-labels`, `menu-filters` and provider tests cover positive, negative, missing, unknown and conflicting labels. Profile states saved preferences do not automatically filter menus. | **Requires external domain/product evidence:** confirm provider-specific semantic mappings with Rutgers or an authorized nutrition reviewer, including remaining untranslated icon variants. Do not market these as allergy-safe filters. No inferred medical safety was added. |
| **F-007 / A007 — Resource bounds** | **Fixed locally.** `ingestion-limits.ts` streams/cancels oversized responses; bounds JSON depth/nodes/strings/collections and food candidates; process-local caches have entry/estimated-byte limits and expire/evict settled work. Pending shared loads are retained; saturated admission fails explicitly. Original request/display deadlines, workers and TTLs remain. Tests cover declared/chunked oversize, malformed structure, 1,501 candidates, independent hall success, shared loads a 100-day cache plateau, and a large seven-day payload conserving all requested-day items. | **Requires external evidence/calibration:** 8 MiB/500,000 nodes for a Nutrislice week, 2 MiB FoodProNet/schools, 256 KiB labels and 1,500 foods per meal need representative production-size validation. The concurrent UI task reported legitimate weekly responses exceeding the first shared 2 MiB/60,000-node limit. No cached body was available here; a synthetic seven-day response above both old limits now preserves 400 requested-day foods per meal. Captured Atrium row counts (338–341) fit the meal cap. No new live provider request was made to certify these limits. Estimated retained bytes exclude JS overhead, Next's own fetch cache and process RSS; host memory/concurrency/rate limits remain unverified. |
| **F-008 / A008 — Browser headers** | **Fixed app baseline.** `next.config.ts` adds CSP, DENY framing, nosniff, referrer and permissions policies, removes framework header, restricts local image inputs/formats and adds image body/disk limits. Browser tests verify headers and fixture navigation/hydration/maps. Production static-profile HEAD already shows Vercel HSTS; app policies have not been deployed. | **Remaining:** inline scripts/styles are allowed for existing Next streaming and positioned maps; CSP is a useful baseline, not complete XSS prevention. Nonce-only scripts would change caching/rendering and are deferred. **External evidence:** verify effective edge overrides and intercepted analytics compatibility after an authorized real build; no analytics event was sent here. |
| **F-009 / A009 — Privacy and data controls** | **Fixed local mechanics and factual notice.** `/privacy`, profile save/clear controls and storage contract describe browser-only preferences, temporary plate, analytics enabled and aggregate ingestion logs. App has no plate/preference analytics calls. Offline tests block analytics entirely and verify clearing does not restore the key. User's analytics-enabled decision is retained; only isolated test artifacts disable analytics. | **Requires product/external decision:** service owner/contact, applicable notice/consent review, analytics/log retention, region and dashboard access, export/retention promises, and intercepted deployed payload canary verification. Access to those account settings was unavailable. The notice is not a legal-compliance certification and must be reviewed against actual settings before broad release. |
| **F-010 / A010 — Accessibility** | **Fixed local behavior.** Native plate dialog with title/description, modal inertness, close control, Escape and explicit boundary focus wrapping for WebKit; named per-item quantity/remove controls; named checked switches; visible focus and reduced-motion treatment. Chromium/WebKit workflows cover Tab/Shift+Tab/Escape/return focus, switches, small width and enlarged text. | **Requires external/manual review:** actual Safari with VoiceOver announcements, system zoom and full contrast review across all map/photo states. Engine automation is not screen-reader testing. |
| **F-011 / A011 — Operations** | **Requires external evidence; local handoff prepared.** Read-only GitHub metadata, branch/rulesets/Actions/environment/deployment metadata and production `/profile` HEAD captured in `remediation-evidence/repository-hosting.json`. `docs/RELEASE_CHECKLIST.md` supplies failure triage and rollback procedure. | Main has no branch protection/rulesets; Production has no review/branch restriction rules. Dependabot security updates are disabled. Vercel IAM/MFA, alias/deployed SHA/runtime, memory/rate limits, log/analytics access, retention, monitoring and rollback were not verified. Operator must inspect/configure through separately authorized work, name owners, accept detection/recovery targets and rehearse a staging outage/rollback. No shared settings changed. |
| **F-012 / A012 — Release gates** | **Fixed local tooling.** ESLint 10 with TypeScript/Next/hooks rules, supported Node pin, typegen/typecheck, offline tests, clean isolated build, Chromium/WebKit fixtures and an unpublished read-only CI workflow with pinned actions. `.env.*` ignored except example. Dependency/native-license inventory and release checklist recorded. Build exposed server-provider imports through client selectors; narrow `menu-helpers.ts` separation and lint restrictions prevent that path. | **Requires external evidence/action:** workflow must run after review and be required by branch/deploy protections; Linux CI has not run. Next 15's legacy lint detector warns that it cannot detect the plugin; standalone ESLint runs successfully and `--print-config` confirms Next rules are active. Deployment provenance/review and native/asset license signoff remain external. |
| **F-013 / A013 — Plate/goals lifecycle** | **Fixed chosen behavior.** User confirmed temporary plates. UI explains reload clearing and snapshots. Preference cross-tab/write/delete behavior is documented and tested. Goal bars clamp at 0–100%, keep numeric overage and missing coverage; unit cases cover 0/100/250%. | Undo was considered but is not required by the chosen temporary lifecycle; it remains an optional product enhancement. No plate persistence, account or database was added. |
| **F-014 / A014 — Status/scope wording** | **Fixed technical wording.** `app/page.tsx`, homepage cards/screen qualify hardcoded schedules as typical/unverified and link Rutgers hours. Menu confirmation remains separate from operating hours; timeouts do not imply no food. Profile clearly distinguishes saved preferences from explicit menu filters. README records lifecycle, limits, runtime and preview QA. Existing station map/food conservation and explicit unavailable states are retained. | **Requires product decision/evidence:** service owner/support contact, public-release stage, official schedule exceptions and asset/license permissions. No invented owner, verified hours, measured-map or Rutgers endorsement claims. |

## Verification results

The final `npm run check` completed successfully with Node **24.21.0**:

- `npm test`: **241 passed**, zero failures, with outbound fetch/HTTP blocked except deliberate fixture stubs.
- `npm run typecheck`: **passed**, including regenerated Next route types.
- `npm run lint`: **passed**, zero warnings; effective ESLint configuration includes the Next rules.
- `npm run build`: **passed** inside `build:isolated` after a clean `npm ci`, followed by isolated TypeScript checks. Next 15 emits its legacy plugin-detection warning; separate lint is authoritative and green. npm also warns about install-script approvals for esbuild/fsevents; installed tools and build worked on this runtime. Linux CI remains to be run.
- `npm run test:browser`: **16 passed**, zero failures across Chromium and WebKit. Includes the concurrent UI task's plate/profile navigation regression. All browser requests are loopback-only, and the test artifact excludes analytics; the normal app keeps analytics enabled.
- `npm audit --audit-level=moderate`: **passed**, zero known vulnerabilities. `git diff --check`: **passed**.

Build ID: `tDAVvhlFyzjLOQt5TvXoP`. Source digest: `a3afb9a4210af3dfc55f92e11dc49c69ea9d9e97397118a82c5d4e972052c130`. All 129 copied source files still matched the working tree at final verification. See [verification.json](remediation-evidence/verification.json), [successful command output](remediation-evidence/release-checks.txt), [browser summary](remediation-evidence/browser-summary.json), and [source hashes](remediation-evidence/source-hashes.json). No baseline file disappeared and no historical audit evidence or artwork changed; [workspace scope](remediation-evidence/workspace-scope.json) distinguishes the preserved pre-edit snapshot.

Tests use synthetic/sanitized fixtures and make no live Rutgers requests or analytics events. The concurrent UI task separately reported live weekly-size failures; the revised weekly budget is covered by synthetic conservation tests, not a claimed live-availability verification. A production compile proves neither upstream availability nor deployed configuration. System browser zoom/actual VoiceOver and full map/photo contrast review remain manual gates; automated checks cover 320px layouts and enlarged text.

## Advisory and dependency evidence

Official Next.js advisories were rechecked September 10 (latest published feed entries August 25), including its [August security release](https://nextjs.org/blog/august-2026-security-release). The selected 15.5.25 maintenance release is beyond the specified 15.5.24 patch boundary. The attacker-controlled AVIF condition was not established in this app: assets are local PNGs, there are no uploads/remote image patterns, and the new local allowlist is `/images/**`. The Windows mixed Pages/App Router condition is absent from this Mac/App-Router-only checkout; deployed OS still needs evidence. These are reachability assessments, not exploitation claims.

The [sharp/libheif advisory](https://github.com/lovell/sharp/security/advisories/GHSA-rgj7-g3m4-5g8c) is patched by installed sharp 0.35.4 with libheif 1.23.2. The actual macOS ARM native library reports libvips 8.18.6, aom 3.15.0 and the complete versions in [dependencies.json](remediation-evidence/dependencies.json). Other deployment architectures must be inspected separately.

React/DOM 19.2.8 was selected alongside patched Next; Next's bundled RSC code is covered by its framework release rather than inferred from root React alone. PostCSS 8.5.28 replaces both root and Next's old nested copy. It exceeds the current traversal/recursion patch boundaries, including [GHSA-fxqj-rqcc-2cmp](https://github.com/advisories/GHSA-fxqj-rqcc-2cmp). Browserslist 4.28.9, baseline-browser-mapping 2.11.22 and postcss-selector-parser 6.1.4 clear the remaining audit findings. Their untrusted CSS/query/statistics input prerequisites are not exposed by this app's static CSS build pipeline. All were patched regardless. [npm-audit.json](remediation-evidence/npm-audit.json) reports zero known vulnerabilities at inspection time; it cannot prove absence of unknown vulnerabilities.

No environment secret values were printed or copied. The production request was HEAD against the static profile route, and the GitHub requests were read-only. Deployment metadata's `Production` label and success state do not establish that the supplied public alias serves that SHA; the record also has `production_environment: false`. The local workflow is not yet an enforced protection rule.

## Scope and release boundary

The implementation keeps `DailyMenu | null` and the real-only shared loader. Null, exceptions and display deadlines produce honest unavailable/unconfirmed output, never sample food or invented timestamps. Mock fixtures/provider remain available only for deliberate direct use. Meal parsing/grouping, station artwork and mapping, search and menu-item conservation retain their existing architecture; quality/destination/budget validation is narrowly added at their boundaries. Early-error ingestion summaries are still synchronously serialized once at daily-loader exit, with unfinished siblings represented honestly and later rejections handled.

The UI overhaul task was concurrently modifying this same checkout. Its account layout/style work was preserved after identifying the overlap; final evidence describes the exact isolated snapshot verified. Future edits must rerun release checks. Historical audit evidence was not rewritten.

**Correctly dated stored-real-menu recovery remains future work.** This pass adds no durable snapshots, stale-data recovery, scheduler, accounts, database, favorites or analytics vendor changes. Feasible local fixes and verification are complete; external/product gates remain explicitly open.
