# Local release checks and operator handoff

The release candidate is committed on the separate local `codex/release-readiness-2026-09-10` branch, with the original UI worktree preserved. It is published as draft PR #1; publication is not production approval. Review the full diff against production and follow the [activation plan](audits/production-readiness-2026-09-10/RELEASE_ACTIVATION_PLAN.md) before any shared action.

## Repeatable local gate

1. Use the preferred Node version in `.nvmrc` and run `npm ci`. Repeat the complete gate on the reviewed hosting floor, Node 24.19.0; see [runtime policy](RUNTIME_POLICY.md).
2. Install test engines with `npx playwright install chromium webkit` (`--with-deps` on Linux CI).
3. Run `npm run check`, `npm audit --audit-level=moderate`, and `git diff --check`.
4. Inspect the finding status in `docs/audits/production-readiness-2026-09-10/REMEDIATION_STATUS.md`. A passing build or synthetic menu does not prove live Rutgers availability.

`build:isolated` makes a fresh temporary source/install directory, runs `npm run build` with outbound application requests blocked, and checks generated route types. It never uses the development `.next` directory. The browser harness serves that artifact with sanitized/synthetic transport fixtures on `127.0.0.1:3217`, includes analytics in the bundle, and intercepts all analytics transport. Do not publish this fixture-tested artifact. A separately authorized real release must build the reviewed source normally with analytics enabled, as the user requested.

The workflow `Release checks` has the unique check/job name `KnightBite release verification`, on Ubuntu 24.04 with read-only repository permissions, no persisted checkout credentials and actions pinned to verified commit SHAs. It also runs on `codex/release-*` pushes and records/asserts the checked SHA. Candidate push, PR test-merge and final main SHA are distinct; Vercel must require the successful main-push check on its own exact deployment commit. See the current status for candidate-specific local verification; the workflow is published and its actual candidate/PR runs are recorded there. The required job now executes the full gate on both 24.21.0 and 24.19.0. Require the exact job from the GitHub Actions app in branch protection and in Vercel Deployment Checks. GitHub's `Production` environment does not gate the current Vercel Git integration. See the [next readiness status](audits/production-readiness-2026-09-10/NEXT_RELEASE_READINESS_STATUS.md) for proposed settings and lockout-safe sequencing.

For a platform-script compatibility check, separately acquire the public `/_vercel/insights/script.js` asset with a read-only GET, decode any HTTP compression, save it **outside the checkout**, and record its timestamp/SHA-256. Run `KNIGHTBITE_ANALYTICS_SCRIPT_FIXTURE=/absolute/path/to/script.js npx playwright test tests/browser/release-evidence.spec.ts`. That command makes no script-download request: both script and event endpoints are fulfilled locally. Ordinary CI uses a labeled synthetic probe, not a claim that the changing vendor script is frozen. Never let canaries reach the real collector. The September 10 captured script passed in Chromium and WebKit; this does not prove future scripts or deployed edge overrides compatible.

## Dependency updates

Review official Next.js, React, sharp and native-library security advisories, plus `npm audit`. Update the lockfile deliberately, use `npm ci` in an isolated directory, and inspect `npm ls next react react-dom sharp postcss --all`. Inspect `require('sharp').versions` on the deployment OS/architecture; root package versions alone do not prove that bundled RSC/native dependencies are patched. Update `.nvmrc` within supported Node LTS when security releases require it. Re-run all gates and regenerate the dependency inventory after lockfile changes. Reassess each advisory's input, router/runtime and OS prerequisites; do not equate a vulnerable package with demonstrated exploitation.

## External release evidence still required

The September 10 dashboard inspection maps `knightbitenb.vercel.app` to the older `70166c4` source and confirms that local patches are not deployed. Every release must repeat the alias/build/check mapping and effective headers/runtime verification. Remaining evidence includes exact deployed Node patch/native libraries, complete token/bypass access inventory, rate/concurrency behavior, and analytics/log data residency and deletion terms. The sole owner’s personal Vercel 2FA is freshly verified; recovery codes were saved according to the user and were never inspected. Team 2FA enforcement was approved, enabled and read back on September 10; recheck member readiness before adding accounts or user-managed pipeline tokens. Product/privacy review must name the service owner/support contact and decide notices, consent and asset permissions. See [manual Safari/VoiceOver QA](ACCESSIBILITY_QA.md); WebKit automation is not that signoff.

Use a separately authorized staging environment for any live integration smoke. First check a successful menu, missing menu, deadline, distinct portions, unknown nutrition, plate behavior, preference save failure/clear, map conservation and keyboard navigation. Analytics compatibility should be checked with intercepted synthetic payloads and verified dashboard configuration before allowing actual test events. Do not include dietary/plate canaries in real analytics or production logs.

## Failure detection and rollback runbook (not rehearsed)

Follow the [Vercel operations runbook](OPERATIONS_RUNBOOK.md) for exact project links, alert specifications, attended-release coverage, triage, Hobby rollback restrictions and the outstanding rehearsal. Native Alerts and Drains require an upgrade on the inspected account; no automated menu-outage alarm is currently established.

Watch `knightbite.menu_ingestion` by hall, requested date, attempt time and outcome. An early-error summary may include unfinished siblings; homepage display timeouts are not provider timeouts. Separate transport, date/context, label enrichment and resource-limit failures. Repeated unknown menus do not prove dining closures. Proposed detection within five minutes of sustained all-hall unavailability and restoration within thirty minutes require operator acceptance; these are not existing guarantees.

Before an authorized release, retain the previous reviewed **patched** artifact and record its source/build identity. This first patched candidate currently has no verified patched production predecessor: the old `70166c4` release is not an approved security fallback. Hobby permits only the immediate predecessor for Instant Rollback. A forward fix and explicit incident/release authorization are necessary if that predecessor is unsuitable. If navigation breaks or truthful menu/plate behavior regresses, stop promotion, compare sanitized ingestion/status evidence, and have the authorized operator restore that known-good artifact through the host's supported rollback mechanism. Never silently restore a version with a reachable critical vulnerability. Recheck headers, menus, storage and analytics behavior after restoration. No database migration or plate recovery is needed; preferences remain schema-compatible. Record one staging outage-detection and rollback rehearsal, elapsed times and owner before declaring this gate complete.

Correctly dated stored-real-menu recovery remains future work. No durable menu snapshots, stale-data recovery, accounts, database, scheduler or monitoring vendor were introduced.
